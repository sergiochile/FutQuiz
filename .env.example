# ============================================================
# El Crack Quiz - Variables de Entorno
# Copia este archivo a .env y rellena los valores reales
# ============================================================

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=8000

# Orígenes permitidos para CORS
# En producción: poner el dominio del frontend de Facebook
# Ejemplo: https://fb.gg,https://apps.facebook.com
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# URL de base de datos
# SQLite (desarrollo local):
DATABASE_URL=sqlite:///./backend/elcrack.db
# PostgreSQL (producción en Railway):
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Entorno de ejecución
ENVIRONMENT=development
# ENVIRONMENT=production
